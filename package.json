{
  "name": "stalefish",
  "dependencies": {
    "envify": "^1.2.1",
    "react": "^0.10.0",
    "lodash": "^2.4.1"
  },
  "devDependencies": {
    "browserify": "^3.44.2",
    "watchify": "^0.8.2"
  },
  "scripts": {
    "build": "browserify src/stalefish.js > dist/stalefish.js",
    "watch-example": "watchify examples/first-example/app.js -o examples/first-example/build.js -d -v"
  }
}
